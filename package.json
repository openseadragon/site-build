{
  "name": "openseadragon-site-build",
  "version": "0.0.1",
  "description": "Website source and build for OpenSeadragon. The version listed here is just to keep NPM happy, but it's not the current OpenSeadragon version (so we don't have to try to keep them in sync).",
  "devDependencies": {
    "grunt": "~0.4.4",
    "grunt-contrib-clean": "~0.5.0",
    "grunt-contrib-connect": "~0.7.1",
    "grunt-contrib-watch": "~0.6.0",
    "grunt-jsdoc": "^2.2.0",
    "marked": "^0.3.9"
  }
}
